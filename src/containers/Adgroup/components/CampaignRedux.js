/**
 * @fileOverview
 * @author crow
 * @time 2017/11/27
 */


let defaultState = {}

export default ( state = defaultState, action ) => {
    switch ( action.type ) {
        default:
            return state
    }
}